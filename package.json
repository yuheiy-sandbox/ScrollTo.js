{
  "name": "scrolltojs",
  "version": "0.1.0",
  "description": "This library supports only smoothly scrolling.",
  "scripts": {
    "start": "babel --blacklist strict src/scrollTo.js -w -o test/scrollTo.js -s",
    "build": "babel --blacklist strict src/scrollTo.js -o scrollTo.js && uglifyjs scrollTo.js -c -m --comments '/\\/*!/' -o scrollTo.min.js"
  },
  "author": "yuhei",
  "license": "MIT",
  "devDependencies": {
    "babel": "^5.8.23",
    "uglifyjs": "^2.4.10"
  }
}
